'use strict';

/**
 * @ngdoc function
 * @name projectsApp.controller:Dashboard
 * @description
 * # Dashboard
 * Controller of the projectsApp
 * */
 angular.module('projectsApp')
 .controller('Dashboard', function ($scope, $timeout, firebaseService) {
    var ref = new Firebase(firebaseService.getFirebBaseURL());




  }); 